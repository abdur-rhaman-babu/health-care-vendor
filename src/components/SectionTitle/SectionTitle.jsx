

const SectionTitle = ({title}) => {
    return (
        <div>
            <h1 className="text-center bg-primary text-white font-bold text-4xl p-4 ">{title}</h1>
        </div>
    );
};

export default SectionTitle;